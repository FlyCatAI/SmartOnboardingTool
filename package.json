{
  "name": "smart-onboarding-tool",
  "private": true,
  "version": "0.0.0",
  "description": "OpenSpec-driven product specs for the RM 展业小程序 (MVP).",
  "scripts": {
    "openspec": "openspec",
    "validate": "openspec validate",
    "validate:strict": "openspec validate --strict"
  },
  "devDependencies": {
    "@fission-ai/openspec": "1.3.1"
  },
  "engines": {
    "node": ">=18"
  }
}
